function showAlert() {
  let strr = "hello";
  strr = "navnesh";
  return alert(strr);
}
// showAlert();

// iterate using for of loop

let stri = "Hello";
for (char of stri) {
  // alert(char);
}

// Accessing character
alert(stri[0]);
alert(stri.at(-1));

let sttr = "Wedget with id hello ";
alert(sttr.indexOf("id"));
